# Revanent does not require secrets for its default local workflow.
# Provider-specific credentials, if later enabled, belong in the environment
# and must never be committed or copied into run artifacts.
